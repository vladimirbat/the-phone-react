{
  "name": "the-phone-commons",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/the-phone-commons/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/the-phone-commons",
        "main": "libs/the-phone-commons/src/index.ts",
        "tsConfig": "libs/the-phone-commons/tsconfig.lib.json",
        "assets": ["libs/the-phone-commons/*.md"]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/the-phone-commons/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/the-phone-commons/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
